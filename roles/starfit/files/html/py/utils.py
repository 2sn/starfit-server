def combine_elements(mode):
    """Preset element combinations"""
    if mode == 1:
        combine = [[6, 7]]
    elif mode == 2:
        combine = [[6, 7, 8]]
    else:
        combine = [[]]

    return combine
